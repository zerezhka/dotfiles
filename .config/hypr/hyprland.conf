# Hyprland configuration
# Based on Sway setup - testing lock screen integration

# Load PC-specific settings (monitors, workspaces)
# Copy local.conf.example to local.conf and customize for your setup
source = ~/.config/hypr/local.conf

# Input configuration
input {
    kb_layout = us,ru
    kb_options = grp:alt_shift_toggle

    follow_mouse = 0

    touchpad {
        natural_scroll = false
    }
}

# General settings
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgb(268bd2)
    col.inactive_border = rgb(073642)

    layout = dwindle
}

# Decorations
decoration {
    rounding = 5

    blur {
        enabled = true
        size = 3
        passes = 1
    }
}

# Animations - minimal for stability
animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 3, myBezier
    animation = windowsOut, 1, 3, default, popin 80%
    animation = border, 1, 5, default
    animation = fade, 1, 3, default
    animation = workspaces, 1, 3, default
}

# Layout
dwindle {
    pseudotile = true
    preserve_split = true
}

# Window rules
windowrule = float on, match:class ^(gsimplecal)$
windowrule = move 1650 500, match:class ^(gsimplecal)$
windowrule = fullscreen on, match:class ^(flux)$
windowrule = fullscreen on, match:title ^(Flux)$


# Mod key
$mod = SUPER

# Keybindings - Basic controls
# [Apps] Launch terminal
bind = $mod, Return, exec, alacritty
# [Windows] Close focused window
bind = $mod SHIFT, Q, killactive
# [Apps] Application launcher
bind = $mod, D, exec, rofi -show drun
# [Help] Show keybindings cheatsheet
bind = $mod, H, exec, ~/.config/hypr/scripts/rofi-hyprland-keybinds-cheatsheet/rofi_keybinds.sh
# [Clipboard] Clipboard history
bind = $mod, C, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy
# [Screenshot] Take screenshot of area
bind = $mod SHIFT, S, exec, sh -c 'geometry=$(slurp) && grim -g "$geometry" - | wl-copy && notify-send "Screenshot saved to clipboard"'
# [Screenshot] Take full screenshot
bind = , Print, exec, sh -c 'grim - | wl-copy && notify-send "Screenshot saved to clipboard"'

# Window management
# [Windows] Toggle fullscreen
bind = $mod, F, fullscreen
# [Windows] Toggle floating mode
bind = $mod SHIFT, Space, togglefloating
# [Windows] Focus last window
bind = $mod, Space, focuscurrentorlast

# Focus movement (vim-like)
# [Focus] Move focus left
bind = $mod, J, movefocus, l
# [Focus] Move focus down
bind = $mod, K, movefocus, d
# [Focus] Move focus up
bind = $mod, L, movefocus, u
# [Focus] Move focus right
bind = $mod, semicolon, movefocus, r

# Focus movement (arrows)
# [Focus] Move focus left
bind = $mod, Left, movefocus, l
# [Focus] Move focus down
bind = $mod, Down, movefocus, d
# [Focus] Move focus up
bind = $mod, Up, movefocus, u
# [Focus] Move focus right
bind = $mod, Right, movefocus, r

# Move windows (vim-like)
# [Windows] Move window left
bind = $mod SHIFT, J, movewindow, l
# [Windows] Move window down
bind = $mod SHIFT, K, movewindow, d
# [Windows] Move window right
bind = $mod SHIFT, semicolon, movewindow, r

# Move windows (arrows)
# [Windows] Move window left
bind = $mod SHIFT, Left, movewindow, l
# [Windows] Move window down
bind = $mod SHIFT, Down, movewindow, d
# [Windows] Move window up
bind = $mod SHIFT, Up, movewindow, u
# [Windows] Move window right
bind = $mod SHIFT, Right, movewindow, r

# Workspace switching
# [Workspaces] Switch to workspace 1-10
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move to workspace
# [Workspaces] Move window to workspace 1-10
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Resize mode
# [Windows] Enter resize mode
bind = $mod, r, exec, ~/.config/hypr/scripts/enter-resize.sh
submap = resize
binde = , J, resizeactive, -50 0
binde = , K, resizeactive, 0 50
binde = , L, resizeactive, 0 -50
binde = , semicolon, resizeactive, 50 0
binde = , Left, resizeactive, -50 0
binde = , Down, resizeactive, 0 50
binde = , Up, resizeactive, 0 -50
binde = , Right, resizeactive, 50 0
bind = , Return, exec, ~/.config/hypr/scripts/exit-resize.sh
bind = , Escape, exec, ~/.config/hypr/scripts/exit-resize.sh
submap = reset

# Lock screen and session
# [Session] Lock screen
bind = $mod SHIFT, L, exec, hyprlock
# [Session] Logout menu
bind = $mod SHIFT, E, exec, ~/.config/hypr/scripts/dmenu-logout.sh
# [Session] Reload Hyprland config
bind = $mod SHIFT, R, exec, hyprctl reload

# Media keys
# [Media] Volume up
bind = , XF86AudioRaiseVolume, exec, ~/.config/sway/scripts/show_notification.sh volume +5%
# [Media] Volume down
bind = , XF86AudioLowerVolume, exec, ~/.config/sway/scripts/show_notification.sh volume -5%
# [Media] Mute/unmute
bind = , XF86AudioMute, exec, ~/.config/sway/scripts/show_notification.sh volume --toggle
# [Media] Play/pause
bind = , XF86AudioPlay, exec, playerctl play-pause

# Mouse bindings
# [Mouse] Move window with mouse
bindm = $mod, mouse:272, movewindow
# [Mouse] Resize window with mouse
bindm = $mod, mouse:273, resizewindow

# Autostart
exec-once = xrdb -merge ~/.Xresources
exec-once = dunst
exec-once = waybar --bar bar-left-top -c ~/.config/waybar/config-left-top.json -s ~/.config/waybar/style.css
exec-once = waybar --bar bar-right-top -c ~/.config/waybar/config-right-top.json -s ~/.config/waybar/style-right-top.css
exec-once = waybar --bar bar-right-vertical -c ~/.config/waybar/config -s ~/.config/waybar/style-right-vertical.css
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = hypridle
exec-once = hyprpaper --config ~/.config/hypr/hyprpaper.conf
exec-once = sleep 1 && hyprctl hyprpaper wallpaper "HDMI-A-2,/usr/share/hypr/wall2.png" && hyprctl hyprpaper wallpaper "HDMI-A-1,/usr/share/hypr/wall2.png"

# Environment
env = XCURSOR_SIZE,24
